- content_for :head do
  %link{:rel => 'stylesheet',:href => '/assets/movie_row_html.sass'}

.row
  - movies.each do |movie|
    .col-lg-3.col-md-6.col-sm-12
      = render partial: "shared/movie_brick", locals: {movie: movie}
